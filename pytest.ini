[pytest]
testpaths = tests
markers =
    unit: Unit tests (no API calls, offline)
    integration: Integration tests (requires FIGMA_ACCESS_TOKEN env var)
    e2e: End-to-end tests (spawns server.py subprocess, needs Python in PATH)
addopts = --tb=short -v
